module Hello
    intent =
        "Demonstrates basic Aver syntax: functions, string interpolation,"
        "local bindings, and verify blocks."

fn greet(name: String) -> String
    ? "Returns a personalised greeting."
    "Hello, {name}"

verify greet
    greet("Alice") => "Hello, Alice"
    greet("World") => "Hello, World"

fn shout(text: String) -> String
    ? "Emphasises a greeting with exclamation marks."
    "{text}!!!"

verify shout
    shout("Hello") => "Hello!!!"
    shout(greet("Alice")) => "Hello, Alice!!!"

fn main() -> Unit
    ! [Console.print]
    greeting = greet("World")
    Console.print(greeting)
    loud = shout(greet("Aver"))
    Console.print(loud)
