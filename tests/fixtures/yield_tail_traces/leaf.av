module Leaf
    intent = "A finite private helper reached through a tail call."
    depends [Pool, Pooled]
    exposes [forward]
    effects [Pool.claim, yield]

fn read(id: Int) -> Int
    ! [Pool.claim, yield]
    Pool.claim(id)

fn forward(id: Int) -> Int
    ! [Pool.claim, yield]
    read(id)
