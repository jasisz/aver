module Main
    depends [Ledger, Pool]
    intent = "A program whose answer functions answer without a reply type."
    effects [Console]

fn main() -> Unit
    ? "Reports that the program is wired."
    ! [Console.print]
    Console.print("answers ready")
