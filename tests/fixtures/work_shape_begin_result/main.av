module Main
    depends [Node, Validation]
    intent = "A program whose job kind returns a bare handle from begin."
    effects [Console]

fn main() -> Unit
    ? "Reports that the job kind is wired."
    ! [Console.print]
    Console.print("jobs ready")
