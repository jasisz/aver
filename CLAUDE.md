# Claude Bootstrap

Canonical agent instructions for this repository live in `AGENTS.md`.

Before making changes, read `AGENTS.md`.

Critical bootstrap rules:
- treat `AGENTS.md` as the source of truth
- prefer `rg` for codebase search
- use `apply_patch` for file edits
- do not revert unrelated user changes
- preserve the existing project architecture and workflow
