module Main
    depends [Ledger, Pool]
    intent = "A program whose answer function performs an effect inside the turn."
    effects [Console]

fn main() -> Unit
    ? "Reports that the program is wired."
    ! [Console.print]
    Console.print("answers ready")
