module Main
    depends [Node, Validation]
    intent = "A job kind whose take does not take the job handle."
    effects [Console]

fn main() -> Unit
    ? "Reports that the job kind is wired."
    ! [Console.print]
    Console.print("jobs ready")
