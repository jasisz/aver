module Main
    depends [Node, Validation]
    intent = "A program whose bound function is not pure."
    effects [Console]

fn main() -> Unit
    ? "Reports that the job kind is wired."
    ! [Console.print]
    Console.print("jobs ready")
