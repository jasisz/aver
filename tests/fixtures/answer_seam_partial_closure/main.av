module Main
    depends [Ledger, Node, Pool, Validation]
    intent = "A program whose job seam is well typed, checked one module at a time."
    effects [Console]

fn main() -> Unit
    ? "Reports that the program is wired."
    ! [Console.print]
    Console.print("answers ready")
