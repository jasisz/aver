# tools/edge — project configuration

[[check.suppress]]
slug = "missing-verify"
files = ["fractal.av"]
fns = ["page"]
reason = """
Fractal.page is a fixed HTML template — pure string assembly with no \
branching logic, parameterised only by a pre-rendered fractal body and \
a millisecond count. Verifying the full HTML literal would add ~2 KB \
of test fixture without exercising any decision; the fractal body it \
wraps is verified through render() coverage."""
