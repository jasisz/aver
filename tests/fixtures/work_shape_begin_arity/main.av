module Main
    depends [Node, Validation]
    intent = "A job kind whose begin takes more than the task."
    effects [Console]

fn main() -> Unit
    ? "Reports that the job kind is wired."
    ! [Console.print]
    Console.print("jobs ready")
