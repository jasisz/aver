module Clock
    kind = capability
    semantics = effectful
    intent = "The tick the ticker waits on."
    exposes [tick]

operation tick() -> Int
    ? "The number of the next tick."
    oracle = generative
    replay = recorded
