module Main
    depends [Ledger, Node, Pool, Validation]
    intent = "A program whose job seam performs an effect."
    effects [Console]

fn main() -> Unit
    ? "Reports that the program is wired."
    ! [Console.print]
    Console.print("answers ready")
