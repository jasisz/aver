module Main
    depends [Ledger, Node, Pool, Validation]
    intent = "A program whose job seam names a module that answers nothing."
    effects [Console]

fn main() -> Unit
    ? "Reports that the program is wired."
    ! [Console.print]
    Console.print("answers ready")
