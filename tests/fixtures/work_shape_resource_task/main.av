module Main
    depends [Node, Validation]
    intent = "A job kind whose task carries a capability resource."
    effects [Console]

fn main() -> Unit
    ? "Reports that the job kind is wired."
    ! [Console.print]
    Console.print("jobs ready")
