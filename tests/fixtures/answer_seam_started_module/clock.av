module Clock
    kind = capability
    semantics = effectful
    intent = "One tick at a time."
    exposes [TickReply, tick]

type TickReply
    Now(Unit)
    Later(Wait.Wake)
    Then(Wait.Wake, Unit)

operation tick() -> Unit
    ? "One tick."
    oracle = generativeOutput
    replay = recorded
