module UserRecord
    intent =
        "Record example with simple user operations."
    exposes [User, greet, isAdult]
    effects [Console]

record User
  name: String
  age: Int

fn greet(u: User) -> String
  ? "Greet a user by name"
  u.name

verify greet
    greet(User(name = "Alice", age = 30)) => "Alice"
    greet(User(name = "Bob", age = 15)) => "Bob"

fn isAdult(u: User) -> Bool
  ? "Check if a user is an adult (18+)"
  u.age >= 18

verify isAdult
    isAdult(User(name = "Alice", age = 30)) => true
    isAdult(User(name = "Bob", age = 15)) => false

fn main()
  ! [Console.print]
  alice = User(name = "Alice", age = 30)
  bob = User(name = "Bob", age = 15)
  Console.print(greet(alice))
  Console.print(greet(bob))
  Console.print(isAdult(alice))
  Console.print(isAdult(bob))
