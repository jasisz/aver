# Aver Context — main.av

_Generated by `aver context`_

selection: mode: `auto`, budget: `24kb`, included_depth: `0`, selected: `131/131`, truncated: `false`, used: `30565b`

---

## Module: Main

> Workflow engine argv entrypoint. Keeps the top-level module thin so the CLI and application core stay in their own modules and `aver context` has a stable root to resume from.

api_effects: `[]`  
module_effects: `[Args.get, Console.print, Console.warn, Disk.appendText, Disk.exists, Disk.makeDir, Disk.readText, Disk.writeText, Time.now]`
main_effects: `[Args.get, Console.print, Console.warn, Disk.appendText, Disk.exists, Disk.makeDir, Disk.readText, Disk.writeText, Time.now]`

---

## Module: Cli

> Thin argv adapter for the workflow engine showcase. CLI parsing and rendering live in separate modules so the reader can understand the core flow without treating the shell adapter as application logic.

effects: `[Args.get, Console.print, Console.warn, Disk.appendText, Disk.exists, Disk.makeDir, Disk.readText, Disk.writeText, Time.now]`

### `runCli() -> Unit`
effects: `[Args.get, Console.print, Console.warn, Disk.appendText, Disk.exists, Disk.makeDir, Disk.readText, Disk.writeText, Time.now]`  
> Starts the workflow engine argv CLI.

---

## Module: Parse

> Plain argv parsing for the workflow engine CLI. The parser stays shell-friendly and returns small typed inputs so the CLI adapter does not need to know any command semantics.


### type Input
`ShowHelp` | `RunCommand(Command)` | `RunQuery(Query)`

### `parseArgs(args: List<String>) -> Result<Input, String>`
> Parses argv-style input into a command, query, or help action.
verify: `parseArgs([]) -> Result.Ok(Input.ShowHelp)`, `parseArgs(["list_tasks"]) -> Result.Ok(Input.RunQuery(Query.ListTasks(Option.None)))`, `parseArgs(["help"]) -> Result.Ok(Input.ShowHelp)`

---

## Module: Commands

> Effectful command handlers for workflow mutations. The app layer is intentionally small: it decides a plan from persisted state, derives follow-up rule effects as data, then commits and dispatches that plan.

effects: `[Console.warn, Disk.appendText, Disk.readText, Disk.writeText, Time.now]`

### type Command
`CreateProject(String, String)` | `ArchiveProject(String)` | `CreateTask(String, String, String, Priority, List<String>, Option<String>)` | `RenameTask(String, String)` | `StartTask(String)` | `BlockTask(String, String)` | `CompleteTask(String)` | `ReopenTask(String, String)` | `ChangePriority(String, Priority)` | `AddTag(String, String)` | `RemoveTag(String, String)` | `AddComment(String, String, String)` | `SetDeadline(String, String)` | `ClearDeadline(String)` | `ArchiveTask(String)` | `RunRules(Option<String>)`

### record CommandReport
`summary: String`, `subjectId: String`, `events: List<TaskEventRecord>`, `notifications: List<Notification>`, `audits: List<AuditEntry>`

### record TaskContext
`project: Project`, `task: Task`, `persistedEvents: List<TaskEventRecord>`, `latestPersistedEvent: Option<TaskEventRecord>`, `nextSeq: Int`

### record ProjectPlan
`summary: String`, `subjectId: String`, `projects: List<Project>`, `audits: List<AuditEntry>`

### record EventPlan
`summary: String`, `subjectId: String`, `events: List<TaskEventRecord>`, `notifications: List<Notification>`, `audits: List<AuditEntry>`

### type CommandPlan
`SaveProjects(ProjectPlan)` | `AppendEvents(EventPlan)`

### `execute(command: Command) -> Result<CommandReport, String>`
effects: `[Console.warn, Disk.appendText, Disk.readText, Disk.writeText, Time.now]`  
> Executes one workflow command through decide, commit, and dispatch.

---

## Module: Types

> Shared domain types for the workflow engine. Keeps records and enums in one place so command, rule, and store modules can speak about the same application core without repeating shapes.


### type TaskStatus
`Todo` | `InProgress` | `Blocked` | `Done` | `Archived`

### type Priority
`Low` | `Normal` | `High` | `Urgent`

### record Project
`id: String`, `name: String`, `archived: Bool`, `createdAt: String`, `updatedAt: String`

### record Comment
`id: String`, `body: String`, `author: String`, `createdAt: String`

### type TaskEvent
`TaskCreated(String, Priority, List<String>, Option<String>, String)` | `TaskRenamed(String, String)` | `TaskStarted(String)` | `TaskBlocked(String, String)` | `TaskCompleted(String)` | `TaskReopened(String, String)` | `PriorityChanged(Priority, String)` | `TagAdded(String, String)` | `TagRemoved(String, String)` | `CommentAdded(Comment)` | `DeadlineSet(String, String)` | `DeadlineCleared(String)` | `TaskArchived(String)`

### record TaskEventRecord
`taskId: String`, `projectId: String`, `seq: Int`, `event: TaskEvent`

### record Task
`id: String`, `projectId: String`, `title: String`, `status: TaskStatus`, `priority: Priority`, `tags: List<String>`, `deadline: Option<String>`, `comments: List<Comment>`, `createdAt: String`, `updatedAt: String`, `statusChangedAt: String`

### record TaskReplayState
`task: Task`, `latestEvent: TaskEventRecord`, `nextSeq: Int`

### record TaskView
`taskId: String`, `projectId: String`, `title: String`, `status: TaskStatus`, `priority: Priority`, `tags: List<String>`, `deadline: Option<String>`, `overdue: Bool`, `deadlineSoon: Bool`, `derivedUrgent: Bool`, `stalled: Bool`, `missingClosingComment: Bool`, `needsReview: Bool`

### record TaskSummary
`taskId: String`, `title: String`, `projectId: String`, `status: TaskStatus`, `priority: Priority`, `derivedUrgent: Bool`, `overdue: Bool`, `stalled: Bool`, `tagSummary: String`, `deadlineText: String`

### record Notification
`key: String`, `taskId: String`, `kind: String`, `message: String`, `createdAt: String`

### record AuditEntry
`key: String`, `taskId: String`, `action: String`, `message: String`, `createdAt: String`

### record RuleResult
`taskId: String`, `events: List<TaskEventRecord>`, `notifications: List<Notification>`, `audits: List<AuditEntry>`, `notes: List<String>`

### record WorkflowSnapshot
`projects: List<Project>`, `taskEvents: List<TaskEventRecord>`, `audits: List<AuditEntry>`, `notifications: List<Notification>`

---

## Module: Projects

> Pure project creation, lookup, and update helpers. Projects stay deliberately shallow: the task engine is event-sourced, but project lifecycle is simple enough to keep as direct state.


### `findProject(projects: List<Project>, projectId: String) -> Option<Project>`
tco: `true`  
> Linear lookup is sufficient for this intentionally small application core.
verify: `findProject([], "p1") -> Option.None`

### `putProject(projects: List<Project>, project: Project) -> List<Project>`
> Replaces an existing project by ID or appends a new one.

### `createProject(id: String, name: String, now: String) -> Result<Project, String>`
> Creates a project after field validation.
verify: `createProject("bad id", "Alpha", "2026-03-08T12:00:00Z") -> Result.Err("Project ID must use letters, numbers, dash, or underscore")`

### `archiveProject(project: Project, now: String) -> Result<Project, String>`
> Archives a project exactly once.

---

## Module: Validation

> Pure validation rules for command inputs and state transitions. Keeps task and project invariants inspectable without touching the store or clock.


### `validTag(tag: String) -> Bool`
> Tags use the same restricted shape as IDs.
verify: `validTag("waiting") -> true`, `validTag("bad tag") -> false`, `validTag("needs_review") -> true`

### `validateProjectCreate(id: String, name: String) -> Result<Unit, String>`
> Checks fields needed to create a project.
verify: `validateProjectCreate("alpha", "   ") -> Result.Err("Project name cannot be blank")`, `validateProjectCreate("bad id", "Alpha") -> Result.Err("Project ID must use letters, numbers, dash, or underscore")`, `validateProjectCreate("alpha_1", "Alpha") -> Result.Ok(Unit)`

### `validateDeadline(deadline: Option<String>) -> Result<Option<String>, String>`
> Validates an optional ISO deadline.
verify: `validateDeadline(Option.None) -> Result.Ok(Option.None)`, `validateDeadline(Option.Some("bad")) -> Result.Err("Invalid deadline: Timestamp must start with YYYY-MM-DDTHH:MM:SS")`, `validateDeadline(Option.Some("2026-03-09T12:00:00Z")) -> Result.Ok(Option.Some("2026-03-09T12:00:00Z"))`

### `validateTaskCreate(id: String, title: String, tags: List<String>, deadline: Option<String>) -> Result<Unit, String>`
> Checks fields needed to create a task.
verify: `validateTaskCreate("task_1", "  ", [], Option.None) -> Result.Err("Task title cannot be blank")`, `validateTaskCreate("bad id", "Write notes", [], Option.None) -> Result.Err("Task ID must use letters, numbers, dash, or underscore")`, `validateTaskCreate("task_1", "Write notes", ["ops", "ops"], Option.None) -> Result.Err("Tags must not repeat")`

### `validateTaskRename(title: String) -> Result<Unit, String>`
> Renames require a non-empty title.
verify: `validateTaskRename(" ") -> Result.Err("Task title cannot be blank")`, `validateTaskRename("New title") -> Result.Ok(Unit)`

### `validateComment(author: String, body: String) -> Result<Unit, String>`
> Comments need both author and body.
verify: `validateComment("", "Looks good") -> Result.Err("Comment author cannot be blank")`, `validateComment("alice", " ") -> Result.Err("Comment body cannot be blank")`, `validateComment("alice", "Looks good") -> Result.Ok(Unit)`

### `validateReason(label: String, reason: String) -> Result<Unit, String>`
> Shared reason validator for block and reopen commands.
verify: `validateReason("Reopen", " ") -> Result.Err("Reopen reason cannot be blank")`, `validateReason("Reopen", "Regression found") -> Result.Ok(Unit)`

### `ensureProjectActive(project: Project) -> Result<Unit, String>`
> Rejects task mutation when the parent project is archived.

### `ensureTransition(from: TaskStatus, to: TaskStatus, taskId: String) -> Result<Unit, String>`
> Turns the transition table into a reviewer-friendly error.
verify: `ensureTransition(Domain.Types.TaskStatus.Done, Domain.Types.TaskStatus.Todo, "t1") -> Result.Err("Task 't1' cannot move from done to todo")`, `ensureTransition(Domain.Types.TaskStatus.Todo, Domain.Types.TaskStatus.InProgress, "t1") -> Result.Ok(Unit)`

---

## Module: Time

> Pure timestamp parsing and comparison for workflow deadlines and rule windows. Uses explicit ISO parsing instead of assuming Time.now() strings can only be compared lexicographically when duration math is needed.


### record Timestamp
`year: Int`, `month: Int`, `day: Int`, `hour: Int`, `minute: Int`, `second: Int`

### `parseIso(s: String) -> Result<Timestamp, String>`
> Parses YYYY-MM-DDTHH:MM:SS and ignores any trailing fractional or zone suffix.
verify: `parseIso("bad") -> Result.Err("Timestamp must start with YYYY-MM-DDTHH:MM:SS")`, `parseIso("2025-02-29T05:00:00Z") -> Result.Err("Invalid calendar timestamp: 2025-02-29T05:00:00")`, `parseIso("2026-03-08T12:30:00Z") -> Result.Ok(Timestamp(year = 2026, month = 3, day = 8, hour = 12, minute = 30, second = 0))`

### `withinNextHours(now: String, target: String, hours: Int) -> Result<Bool, String>`
> True when target is in the future and inside the requested hour window.
verify: `withinNextHours("2026-03-08T12:00:00Z", "2026-03-10T12:00:00Z", 24) -> Result.Ok(false)`, `withinNextHours("2026-03-08T12:00:00Z", "2026-03-08T23:00:00Z", 24) -> Result.Ok(true)`, `withinNextHours("2026-03-08T12:00:00Z", "2026-03-08T11:00:00Z", 24) -> Result.Ok(false)`

### `olderThanDays(from: String, to: String, days: Int) -> Result<Bool, String>`
> True when the elapsed time is strictly greater than N days.
verify: `olderThanDays("2026-03-01T12:00:00Z", "2026-03-05T12:00:01Z", 3) -> Result.Ok(true)`, `olderThanDays("2026-03-01T12:00:00Z", "2026-03-04T12:00:00Z", 3) -> Result.Ok(false)`

### `isPast(target: String, now: String) -> Result<Bool, String>`
> True when the target timestamp is before the supplied now timestamp.
verify: `isPast("2026-03-07T12:00:00Z", "2026-03-08T12:00:00Z") -> Result.Ok(true)`, `isPast("2026-03-09T12:00:00Z", "2026-03-08T12:00:00Z") -> Result.Ok(false)`

---

## Module: Tasks

> Pure task command logic that produces explicit events. Command handlers stay thin because this module converts validated user intent into domain events without touching persistence or notifications.


### `nextSeq(events: List<TaskEventRecord>) -> Int`
> Event sequence is one-based and local to a task stream.
verify: `nextSeq([]) -> 1`, `nextSeq([TaskEventRecord(taskId = "t1", projectId = "p1", seq = 1, event = Domain.Types.TaskEvent.TaskStarted("2026-03-08T12:00:00Z"))]) -> 2`

### `createTask(taskId: String, projectId: String, title: String, priority: Priority, tags: List<String>, deadline: Option<String>, now: String) -> Result<TaskEventRecord, String>`
> Creates the first event in a task stream.

### `renameTask(task: Task, seq: Int, title: String, now: String) -> Result<TaskEventRecord, String>`
> Renames a task unless it has already been archived.

### `startTask(task: Task, seq: Int, now: String) -> Result<TaskEventRecord, String>`
> Moves a task into in-progress work.

### `blockTask(task: Task, seq: Int, reason: String, now: String) -> Result<TaskEventRecord, String>`
> Marks a task blocked with an explicit reason.
verify: `blockTask(sampleTask(), 2, " ", "2026-03-09T12:00:00Z") -> Result.Err("Block reason cannot be blank")`

### `completeTask(task: Task, seq: Int, now: String) -> Result<TaskEventRecord, String>`
> Moves a task into done.

### `reopenTask(task: Task, seq: Int, reason: String, now: String) -> Result<TaskEventRecord, String>`
> Reopens a done task into active work.
verify: `reopenTask(sampleTask(), 4, "Regression found", "2026-03-11T12:00:00Z") -> Result.Err("Task 't1' can only be reopened from done")`

### `changePriority(task: Task, seq: Int, priority: Priority, now: String) -> Result<TaskEventRecord, String>`
> Updates priority unless the value is unchanged.
verify: `changePriority(sampleTask(), 2, Domain.Types.Priority.Normal, "2026-03-09T12:00:00Z") -> Result.Err("Task 't1' already has priority normal")`

### `addTag(task: Task, seq: Int, tag: String, now: String) -> Result<TaskEventRecord, String>`
> Adds a tag if it is valid and not already present.
verify: `addTag(Task.update(sampleTask(), tags = ["waiting"]), 2, "waiting", "2026-03-09T12:00:00Z") -> Result.Err("Task 't1' already has tag 'waiting'")`

### `removeTag(task: Task, seq: Int, tag: String, now: String) -> Result<TaskEventRecord, String>`
> Removes a tag only when it currently exists.
verify: `removeTag(sampleTask(), 2, "waiting", "2026-03-09T12:00:00Z") -> Result.Err("Task 't1' does not have tag 'waiting'")`

### `addComment(task: Task, seq: Int, author: String, body: String, now: String) -> Result<TaskEventRecord, String>`
> Adds a comment with a deterministic ID derived from the task stream.
verify: `addComment(sampleTask(), 3, "", "Waiting on review", "2026-03-09T12:00:00Z") -> Result.Err("Comment author cannot be blank")`

### `setDeadline(task: Task, seq: Int, deadline: String, now: String) -> Result<TaskEventRecord, String>`
> Sets or replaces a task deadline.
verify: `setDeadline(sampleTask(), 2, "bad", "2026-03-09T12:00:00Z") -> Result.Err("Invalid deadline: Timestamp must start with YYYY-MM-DDTHH:MM:SS")`

### `clearDeadline(task: Task, seq: Int, now: String) -> Result<TaskEventRecord, String>`
> Clears an existing deadline.
verify: `clearDeadline(sampleTask(), 2, "2026-03-09T12:00:00Z") -> Result.Err("Task 't1' has no deadline to clear")`

### `archiveTask(task: Task, seq: Int, now: String) -> Result<TaskEventRecord, String>`
> Moves a task into its final archived state.

---

## Module: Events

> Pure event application and replay logic for workflow tasks. With typed sum payload matching now working across the toolchain, replay can live in the domain again instead of reconstructing state from serialized event fields.


### `eventName(event: TaskEvent) -> String`
> Stable event kind label for rendering and audit helpers.
verify: `eventName(Domain.Types.TaskEvent.TaskReopened("Regression found", "2026-03-10T12:00:00Z")) -> "TaskReopened"`, `eventName(Domain.Types.TaskEvent.TaskCreated("Plan release", Domain.Types.Priority.High, ["ops"], Option.None, "2026-03-08T12:00:00Z")) -> "TaskCreated"`

### `recordedAt(event: TaskEvent) -> String`
> Extracts the effective timestamp from each event.
verify: `recordedAt(Domain.Types.TaskEvent.TaskCompleted("2026-03-09T12:00:00Z")) -> "2026-03-09T12:00:00Z"`

### `latestEvent(events: List<TaskEventRecord>) -> Option<TaskEventRecord>`
> Reads the last event in a task stream or flat log.
verify: `latestEvent([]) -> Option.None`, `latestEvent([sampleCreatedEvent(), sampleCompletedEvent()]) -> Option.Some(sampleCompletedEvent())`

### `eventsForTask(events: List<TaskEventRecord>, taskId: String) -> List<TaskEventRecord>`
> Filters a flat event log down to one task without growing the runtime stack with the log size.
verify: `eventsForTask([sampleCreatedEvent()], "t2") -> []`

### `hasTaskEvents(events: List<TaskEventRecord>, taskId: String) -> Bool`
tco: `true`  
> True when the flat event log contains at least one event for the given task.
verify: `hasTaskEvents([sampleCreatedEvent()], "t1") -> true`, `hasTaskEvents([sampleCreatedEvent()], "t2") -> false`

### `findTask(tasks: List<Task>, taskId: String) -> Option<Task>`
tco: `true`  
> Linear lookup for replayed tasks.
verify: `findTask([], "t1") -> Option.None`, `findTask([sampleTask()], "t1") -> Option.Some(sampleTask())`

### `replayTask(events: List<TaskEventRecord>) -> Result<Task, String>`
> Replays one task stream into its current projection.
verify: `replayTask([]) -> Result.Err("No events for task")`

### `replayAll(events: List<TaskEventRecord>) -> Result<List<Task>, String>`
> Replays the flat task event log into current task projections.

### `replayAllStates(events: List<TaskEventRecord>) -> Result<List<TaskReplayState>, String>`
> Replays the flat task event log into current task projections plus per-task metadata.

---

## Module: Rules

> Explicit workflow rule functions over replayed task state. Rules stay as plain functions rather than a hidden mini-language so reviewers can see which business pressure caused each follow-up event or notification.


### `runRules(project: Project, task: Task, latest: Option<TaskEventRecord>, now: String, nextSeq: Int) -> Result<RuleResult, String>`
> Runs all explicit workflow rules for one task.

---

## Module: Views

> Pure derived views and summaries over replayed task state. Deadlines, stalled work, and review-needed markers stay derived here instead of being hidden as imperative mutations.


### `statusText(status: TaskStatus) -> String`
> Readable task status label.
verify: `statusText(Domain.Types.TaskStatus.Done) -> "done"`, `statusText(Domain.Types.TaskStatus.Blocked) -> "blocked"`

### `priorityText(priority: Priority) -> String`
> Readable priority label.
verify: `priorityText(Domain.Types.Priority.High) -> "high"`, `priorityText(Domain.Types.Priority.Urgent) -> "urgent"`

### `deriveView(task: Task, now: String) -> Result<TaskView, String>`
> Builds derived flags without mutating stored priority or tags.

### `deriveSummary(task: Task, now: String) -> Result<TaskSummary, String>`
> Compact list view over a task.

---

## Module: Store

> Flat-file persistence for projects and append-only task events. This module owns file formats and append/write primitives; replay stays in the domain so the app core reads like event sourcing instead of storage magic.

effects: `[Disk.appendText, Disk.exists, Disk.makeDir, Disk.readText, Disk.writeText]`

### `dataDir() -> String`
> Base directory for workflow engine files.
verify: `dataDir() -> "/tmp/aver_workflow_engine"`

### `initStore() -> Result<Unit, String>`
effects: `[Disk.appendText, Disk.exists, Disk.makeDir, Disk.writeText]`  
> Ensures the storage directory and core files exist.

### `loadProjects() -> Result<List<Project>, String>`
effects: `[Disk.readText]`  
> Reads all projects from disk.

### `saveProjects(projects: List<Project>) -> Result<Unit, String>`
effects: `[Disk.writeText]`  
> Rewrites project state as one whole file.

### `loadTaskEvents() -> Result<List<TaskEventRecord>, String>`
effects: `[Disk.readText]`  
> Reads the append-only task event log.

### `appendTaskEvents(entries: List<TaskEventRecord>) -> Result<Unit, String>`
effects: `[Disk.appendText]`  
> Appends many serialized task events in one write.

---

## Module: Codec

> Shared flat-file codec helpers for the workflow engine infra modules. The storage format is intentionally plain text, so escaping, optional fields, and line splitting stay explicit instead of being hidden in a serializer.


### `escapeField(text: String) -> String`
> Escapes percent, pipe, and newline for one flat-file field.
verify: `escapeField("a|b") -> "a%7Cb"`, `escapeField("plain") -> "plain"`, `escapeField("line
break") -> "line%0Abreak"`

### `serializeBool(value: Bool) -> String`
> Stable file representation for booleans.
verify: `serializeBool(true) -> "true"`, `serializeBool(false) -> "false"`

### `parseBool(raw: String) -> Result<Bool, String>`
> Parses one stored boolean field.
verify: `parseBool("x") -> Result.Err("Invalid bool: x")`, `parseBool("true") -> Result.Ok(true)`, `parseBool("false") -> Result.Ok(false)`

### `fieldAt(parts: List<String>, index: Int, label: String) -> Result<String, String>`
> Reads and unescapes one required field from a split line.
verify: `fieldAt([], 0, "project") -> Result.Err("Corrupt project: missing field 0")`, `fieldAt(["a%7Cb"], 0, "project") -> Result.Ok("a|b")`, `fieldAt(["a", "b"], 1, "project") -> Result.Ok("b")`

### `optionalFieldAt(parts: List<String>, index: Int) -> String`
> Returns one optional raw field or '-' when it is missing.
verify: `optionalFieldAt(["a"], 1) -> "-"`, `optionalFieldAt(["a", "b"], 1) -> "b"`

### `encodeOption(value: Option<String>) -> String`
> Stores optional text as '-' or an escaped field.
verify: `encodeOption(Option.None) -> "-"`, `encodeOption(Option.Some("a|b")) -> "a%7Cb"`

### `decodeOption(raw: String) -> Option<String>`
> Reads optional text stored by encodeOption.
verify: `decodeOption("-") -> Option.None`, `decodeOption("a%7Cb") -> Option.Some("a|b")`

### `splitNonEmptyLines(content: String) -> List<String>`
> Splits text into lines and drops empty ones.
verify: `splitNonEmptyLines("") -> []`, `splitNonEmptyLines("a
b
") -> ["a", "b"]`

---

## Module: Clock

> Thin time adapter for the workflow engine. Keeping Time.now behind one module makes orchestration explicit about where time enters the system without contaminating pure domain logic.

effects: `[Time.now]`

### `now() -> String`
effects: `[Time.now]`  
> Returns the current timestamp as the orchestration layer sees it.

---

## Module: Notify

> Notification delivery and persistence for workflow rules. Notifications are stored so queries can inspect them later, but they are also emitted immediately to the console to make the CLI feel alive.

effects: `[Console.warn, Disk.appendText, Disk.exists, Disk.makeDir, Disk.readText, Disk.writeText]`

### `initNotify() -> Result<Unit, String>`
effects: `[Disk.appendText, Disk.exists, Disk.makeDir, Disk.writeText]`  
> Ensures the notification file exists.

### `loadNotifications() -> Result<List<Notification>, String>`
effects: `[Disk.readText]`  
> Reads all persisted notifications.

### `forTask(entries: List<Notification>, taskId: String) -> List<Notification>`
> Filters notifications down to one task.

### `emitNotifications(entries: List<Notification>) -> Result<Unit, String>`
effects: `[Console.warn, Disk.appendText, Disk.readText]`  
> Persists and prints only new notifications.

---

## Module: Audit

> Audit trail persistence for workflow actions and rule follow-ups. Audit entries stay outside the task event log because some audit records explain why a decision happened rather than representing state reconstruction inputs.

effects: `[Disk.appendText, Disk.exists, Disk.makeDir, Disk.readText, Disk.writeText]`

### `initAudit() -> Result<Unit, String>`
effects: `[Disk.appendText, Disk.exists, Disk.makeDir, Disk.writeText]`  
> Ensures the audit file exists.

### `loadEntries() -> Result<List<AuditEntry>, String>`
effects: `[Disk.readText]`  
> Reads all persisted audit entries.

### `forSubject(entries: List<AuditEntry>, subjectId: Option<String>) -> List<AuditEntry>`
> Optional subject filter for audit queries.

### `recordEntries(entries: List<AuditEntry>) -> Result<Unit, String>`
effects: `[Disk.appendText, Disk.readText]`  
> Appends only new audit entries, preserving the existing log.

---

## Module: Queries

> Effectful read-model assembly for the workflow engine CLI. Queries load persisted state, replay tasks, and derive views without mixing those concerns into the parser or the pure domain modules.

effects: `[Disk.readText, Time.now]`

### type Query
`ListTasks(Option<String>)` | `ShowTask(String)` | `ShowAudit(Option<String>)`

### record TaskDetails
`project: Project`, `task: Task`, `view: TaskView`, `history: List<TaskEventRecord>`, `audits: List<AuditEntry>`, `notifications: List<Notification>`

### type QueryOutput
`TaskList(List<TaskSummary>)` | `TaskDetail(TaskDetails)` | `AuditTrail(List<AuditEntry>)`

### `run(query: Query) -> Result<QueryOutput, String>`
effects: `[Disk.readText, Time.now]`  
> Executes one read-only query over persisted workflow state.

---

## Module: Render

> Shell-friendly rendering for workflow engine command and query results. Formatting stays dumb and explicit so the interesting orchestration remains in the app and domain layers, not in presentation glue.


### `renderCommand(result: Result<CommandReport, String>) -> Result<List<String>, String>`
> Maps command execution into rendered output.
verify: `renderCommand(Result.Err("boom")) -> Result.Err("boom")`

### `renderQuery(result: Result<QueryOutput, String>) -> Result<List<String>, String>`
> Maps query execution into rendered output.
verify: `renderQuery(Result.Err("boom")) -> Result.Err("boom")`, `renderQuery(Result.Ok(QueryOutput.AuditTrail([]))) -> Result.Ok(["audits: 0"])`

---

## Decisions

### OneRulePassPerCommand (2026-03-08)
**Chosen:** "SingleExplicitRulePass" — **Rejected:** "LoopUntilNoRulesFire", "HideRulesInsideStoreWrites"
> Workflow rules should stay reviewable, not turn into an implicit fixpoint engine. A single pass after each user mutation keeps command flow explicit even if it…
impacts: `execute`, `decide`, `planTaskCommand`, `deriveRuleEffectsAfterPrimaryEvent`, `commit`, `dispatch`

### ProjectsStayStateBased (2026-03-08)
**Chosen:** "StateRecordsForProjects" — **Rejected:** "ProjectEventSourcing", "SingleGlobalAggregate"
> Project transitions are shallow compared with task lifecycle. A separate project event log would add ceremony without revealing new pressure. Keeping projects a…
impacts: `createProject`, `archiveProject`, `putProject`

### ArchivedProjectsRejectTaskChanges (2026-03-08)
**Chosen:** "RejectTaskMutationWhenProjectArchived" — **Rejected:** "SoftWarningOnly", "ImplicitOverride"
> Archived projects are an operational boundary, not only a display state. Once a project is archived, task mutation should stop unless a future command makes tha…
impacts: `ensureProjectActive`

### CommandsProduceEventsNotMutatedTasks (2026-03-08)
**Chosen:** "CommandFunctionsEmitEvents" — **Rejected:** "MutateTaskRecordsDirectly", "HideStateChangeInsideStore"
> A task workflow engine should make state transitions reviewable at the event level. Returning events instead of directly mutated tasks keeps replay and audit al…
impacts: `createTask`, `renameTask`, `completeTask`, `reopenTask`

### ReplayReturnsToDomain (2026-03-08)
**Chosen:** "TypedReplayInDomain" — **Rejected:** "KeepReplayInInfraStore", "DeleteReplayHelpers"
> Replay belongs with the domain semantics, not inside the storage adapter. Now that constructor payload matching is reliable enough, the natural typed version is…
impacts: `replayTask`, `replayAll`, `applyEvent`

### RulesStayAsFunctions (2026-03-08)
**Chosen:** "PlainRuleFunctions" — **Rejected:** "RuleDsl", "RulesInsideCommandHandlers"
> The rule count is small enough that a DSL would mostly hide branching. Explicit functions keep rule-specific edge cases near the code they affect.
impacts: `runRules`

### DeadlinesProjectUrgencyInsteadOfMutatingPriority (2026-03-08)
**Chosen:** "DerivedUrgencyAndNotifications" — **Rejected:** "RewritePriorityToUrgent", "IgnoreDeadlinePressure"
> Stored priority represents intent; deadline pressure is situational. Mutating priority during rule evaluation would erase who set the original priority.
impacts: `ruleDeadlineSoon`, `ruleOverdue`

### AppendOnlyTaskEventsWithFlatProjectState (2026-03-08)
**Chosen:** "TaskEventsPlusProjectState" — **Rejected:** "EverythingAsMutableState", "EverythingEventSourced", "InMemoryOnly"
> Task lifecycle is where reviewable history matters, so an append-only event log pays for itself. Projects are much shallower, and storing them as current state…
impacts: `loadProjects`, `saveProjects`, `loadTaskEvents`, `appendTaskEvents`

### TimeEntersThroughClockModule (2026-03-08)
**Chosen:** "SingleClockAdapter" — **Rejected:** "CallTimeNowEverywhere", "ThreadNowAsUntypedStringByConvention"
> Deadline and stalled rules depend on the current time, but that dependency should remain visible at the app boundary instead of leaking into pure domain modules…
impacts: `now`

### NotificationsDeduplicateByKey (2026-03-08)
**Chosen:** "PersistAndSkipKnownKeys" — **Rejected:** "AlwaysEmitAgain", "NoNotificationHistory"
> Deadline and overdue rules can be re-run manually, so delivery needs a stable idempotency key or the CLI will drown in repeated warnings.
impacts: `emitNotifications`, `loadNotifications`

### AuditsStayAdjacentToEventsNotInsideThem (2026-03-08)
**Chosen:** "SeparateAuditLog" — **Rejected:** "EncodeAllAuditNarrativeAsTaskEvents", "NoAuditTrail"
> Reopen reasons and command traces are valuable to reviewers, but they should not change replay semantics for task state.
impacts: `recordEntries`, `loadEntries`

### QueriesReplayInsteadOfReadingMutableTaskTables (2026-03-08)
**Chosen:** "ReplayForReadModel" — **Rejected:** "DedicatedMutableTaskProjectionFile", "AdHocTaskCacheOnly"
> The whole point of the workflow engine is to see whether replay stays readable. If queries bypass replay with a separate mutable task table, that test becomes f…
impacts: `run`

