[providers]
schema = 1

# A job kind: the runtime runs Ledger.validate off the turn. Ledger begins
# each validation itself when a body is delivered, and the walk's request
# for the next target parks on that job until it lands.
[[providers.bindings]]
capability = "Validation"
work = "Ledger.validate"

[work]
max-jobs = 1
