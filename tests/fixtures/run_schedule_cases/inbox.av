module Inbox
    intent = "Each process asks to record its own id once."
    kind = capability
    semantics = effectful
    exposes [arrive]

operation arrive(id: Int) -> Unit
    ? "Record which process was served."
    oracle = generative
    replay = recorded
