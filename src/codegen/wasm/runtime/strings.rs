//! String runtime helpers — every body has been migrated to a
//! `runtime/wat/*.part.wat` fragment. Kept as a stub so its
//! `mod strings;` declaration in `runtime/mod.rs` still resolves;
//! new string helpers that prefer Rust-side construction can land
//! here.
