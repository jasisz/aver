module Wire
    kind = capability
    semantics = effectful
    depends [Wait]
    intent = "Fixture capability under slice/: one frame at a time; its operation names its own type."
    exposes [Heard, frame]

type Heard
    Message(String)
    Gone(String)

operation frame() -> Heard
    ? "The next frame."
    oracle = generative
    replay = recorded
