module User
    exposes [nameById]
    intent =
        "User lookup module for dot-path import demo."

fn nameById(id: Int) -> String
    ? "Lookup demo user name by id."
    = match id
        1 -> "Ada"
        _ -> "Unknown"

verify nameById
    nameById(1) => "Ada"
    nameById(2) => "Unknown"
