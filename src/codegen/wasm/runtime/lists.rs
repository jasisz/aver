//! Linked-list runtime helpers — all bodies migrated to
//! `runtime/wat/{list_cons,lists}.part.wat`. Kept as a stub so its
//! `mod lists;` declaration in `runtime/mod.rs` still resolves; new
//! list helpers that prefer Rust-side construction can land here.
