module Said
    intent = "Fixture answer module: the answers this program gives to Say, from one state."
    depends [Say, Wait]
    effects []

record State
    line: String

fn print(state: State, message: String) -> Tuple<State, Say.PrintReply>
    ? "Takes the line the process wrote."
    (state, Say.PrintReply.Now(Unit))

fn readLine(state: State) -> Tuple<State, Say.ReadLineReply>
    ? "Hands the process the line this state holds."
    (state, Say.ReadLineReply.Now(Result.Ok(state.line)))
