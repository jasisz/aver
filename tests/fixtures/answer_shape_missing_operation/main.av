module Main
    depends [Ledger, Pool]
    intent = "A program whose answer module answers only half of the capability."
    effects [Console]

fn main() -> Unit
    ? "Reports that the program is wired."
    ! [Console.print]
    Console.print("answers ready")
