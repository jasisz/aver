module Main
    depends [Chain, Ledger, Pool]
    intent = "A program whose two answered capabilities share an operation name."
    effects [Console]

fn main() -> Unit
    ? "Reports that the program is wired."
    ! [Console.print]
    Console.print("answers ready")
