module MapKeys
    intent =
        "Reads a tuple-keyed map in iteration order, for a caller in another module."
    effects []

fn floatValues(m: Map<Tuple<Int, Int>, Int>) -> List<Int>
    ? "Values of a tuple-keyed map, in iteration order."
    Map.values(m)
